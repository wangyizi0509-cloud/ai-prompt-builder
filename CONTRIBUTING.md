# 项目贡献与分支管理指南

为了保证代码质量和持续交付（CD）的稳定性，本项目采用以下分支管理策略。

## 分支模型

本项目主要使用 `main`、`develop` 和 `feature/*` 三类分支。

### 1. `main` 分支
- **角色**：线上基线分支，代表生产环境的稳定代码。
- **合并规则**：
    - **只接受**来自 `develop` 分支的合并请求（Pull Request）。
    - **严禁直接 commit** 到 `main` 分支。
- **持续部署 (CD)**：任何对 `main` 分支的变更都会触发自动持续部署流程。

### 2. `develop` 分支
- **角色**：集成分支，对应测试环境。
- **合并规则**：
    - 所有的功能开发分支（`feature/*`）在完成后最终都合入这里。
    - 定期将测试稳定的 `develop` 代码合并至 `main` 分支。

### 3. `feature/*` 分支
- **角色**：功能开发分支，用于新功能开发或 Bug 修复。
- **工作流**：
    - 从最新的 `develop` 分支拉出：`git checkout -b feature/your-feature-name develop`
    - 开发完成后，通过 **Pull Request (PR)** 的形式合回 `develop` 分支。
    - PR 合并后，该分支应被删除。

---

## 开发流程建议

1.  **同步代码**：在开始开发前，确保你的 `develop` 分支是最新的。
2.  **创建分支**：命名规范为 `feature/功能描述` 或 `fix/缺陷描述`。
3.  **提交代码**：提交信息应清晰明了。
4.  **发起 PR**：
    - 合并目标应选择 `develop` 分支。
    - PR 中应包含简单的变更说明。
    - 建议通过相关测试后再发起 PR。
5.  **代码评审**：鼓励团队成员之间进行代码评审（Code Review）。

## 持续部署 (CD) 说明

目前项目的 `main` 分支已配置自动 CD。当 `develop` 的代码经过测试确认无误并合并到 `main` 后，系统会自动触发部署流程，将变更同步至生产环境。请在合并至 `main` 前务必确保代码的稳定性。
