# Crushe 数据仪表盘使用说明

> 这是本地数据埋点系统的日常使用手册。想看漏斗 / 事件 / LLM 健康 / 推送飞书，全在这一页。

---

## 快速开始（3 步）

在终端里，先 `cd` 到项目根目录：

```bash
cd /Users/ant/Crushe/模型策略
```

然后：

```bash
# 第 1 步：启服务（电脑重启后要跑一次）
bash scripts/daily_dashboard.sh start

# 第 2 步：打开仪表盘
bash scripts/daily_dashboard.sh open

# 第 3 步：不用了可以停服务
bash scripts/daily_dashboard.sh stop
```

---

## 日常常用命令

> 所有命令的入口都是 `bash scripts/daily_dashboard.sh <命令>`

### 服务管理

| 命令 | 作用 |
|---|---|
| `start` | 启动 LangGraph + FastAPI（开机后首次用要跑）|
| `stop` | 停止服务 |
| `status` | 查服务是否在跑 |
| `open` | 浏览器自动打开仪表盘 |

### 查数据（前提：服务已 start）

| 命令 | 作用 | 例子 |
|---|---|---|
| `funnel [range]` | 看漏斗 | `funnel today` / `funnel yesterday` / `funnel 7d` |
| `events [N]` | 看最近 N 条事件 | `events 50` |
| `llm [range]` | 看 LLM 健康 | `llm 7d` / `llm yesterday` |

**range 可选值**：`today`（今日实时）/ `yesterday`（昨日）/ `7d`（近 7 天）

### 推飞书

| 命令 | 作用 |
|---|---|
| `feishu` | 推昨天数据到飞书群 |
| `feishu 2026-04-22` | 推指定日期 |

### 帮助

```bash
bash scripts/daily_dashboard.sh help   # 查看所有命令
```

---

## 数据看哪里？3 个渠道对比

| 渠道 | 地址/方式 | 适合什么时候 |
|---|---|---|
| **数据仪表盘** | http://localhost:8000/dashboard.html | 日常首选，图形化查看 |
| **终端命令** | `funnel` / `events` / `llm` | 快速扫一眼数据 |
| **飞书消息** | 群机器人推送 | 每天早上主动看昨日数据 |

3 个渠道**数据一致**（都来自 Supabase 的 `events` 表）。

---

## 仪表盘各 Tab 说明

打开 http://localhost:8000/dashboard.html 后：

| Tab | 内容 |
|---|---|
| **今日实时** | 今天（北京时间 00:00 至此刻）的 11 步漏斗 |
| **昨日** | 昨日完整漏斗 |
| **近 7 天** | 近 7 天累计漏斗 |
| **最近 100 条** | 事件明细表格（时间、事件名、anon、props）|
| **LLM 健康** | analyze / report 两个端点的近 7 天成功率 + 平均耗时 |

---

## 漏斗的 11 步

1. **Splash 曝光**（打开落地页）
2. **提交描述+截图**
3. **回答 A1**（怎么认识）
4. **回答 A2**（认识多久）
5. **回答 A3**（做过什么）
6. **回答 A4**（遇到什么）
7. **回答 A5**（想解决什么）
8. **完成题库**
9. **看到报告**
10. **付费成功**（目前是 mock 付费）
11. **发首条消息**（进入主对话）

> 每步按 `anonymous_id`（设备维度）去重。未登录用户用 localStorage 的 UUID 区分。

---

## LangSmith（模型调用追踪）

**用途**：查每一次 LLM 调用的 prompt、回答、耗时、错误——排查模型路径问题时用。

**访问**：https://smith.langchain.com → 左侧 Projects → 点 `crushe-agent-debug`

**自动追踪的场景**：
- Onboarding 的 analyze 端点（首发钩子 + 筛题）
- Onboarding 的 report 端点（诊断报告）
- 主对话的 main_agent 所有 LLM 调用

**不需要配置，服务起着就自动上报**。

---

## 常见问题

### Q1：跑 `funnel` 提示 "FastAPI 未启动"？

先跑 `bash scripts/daily_dashboard.sh start`，等服务起来再查。

### Q2：仪表盘打开是空的/全 0？

正常的。漏斗数据来自真实用户访问前端（`/splash.html`、`/onboarding.html` 等），如果没人访问就是 0。

**自己测试**：
- 用 Chrome 无痕模式打开 http://localhost:8000/splash.html
- 走一遍完整流程（填描述、上传截图、答题、看报告、付费、发消息）
- 回仪表盘点 "今日实时" → 应该看到漏斗被填满

### Q3：LLM 调用慢/失败，怎么办？

1. 打开 LangSmith 的 `crushe-agent-debug` 项目看最近失败的 trace，找错误原因
2. 仪表盘 "LLM 健康" tab 看成功率——低于 80% 通常是 LLM 服务异常
3. `analyze_fallback` 事件表示 analyze 调用失败走了降级（前端仍能看到兜底钩子，不阻断用户）

### Q4：飞书群没收到消息？

- 检查 webhook 还有没有（飞书群设置 → 群机器人 → 有没有被删）
- 运行 `bash scripts/daily_dashboard.sh feishu` 手动推一条测试
- 看终端报错：常见是 Supabase 或飞书网络超时，再跑一次就行

### Q5：想用 SQL 直接查数据？

去 Supabase → SQL Editor，跑：

```sql
-- 按事件名看总量
SELECT event_name, count(*), count(DISTINCT anonymous_id) AS users
FROM events
WHERE created_at > now() - interval '7 days'
GROUP BY 1
ORDER BY 2 DESC;

-- 单个用户的完整轨迹
SELECT event_name, props, created_at
FROM events
WHERE anonymous_id = '<粘贴一个 anonymous_id>'
ORDER BY created_at;

-- 今日漏斗（北京时间）
SELECT event_name, count(DISTINCT anonymous_id) AS users
FROM events
WHERE created_at >= (date_trunc('day', now() AT TIME ZONE 'Asia/Shanghai')) AT TIME ZONE 'Asia/Shanghai'
  AND event_name IN ('splash_view','description_submit','onboarding_complete','report_view','pay_success','first_message_send')
GROUP BY 1;
```

---

## 关键文件位置

| 路径 | 用途 |
|---|---|
| `scripts/daily_dashboard.sh` | 唯一命令入口 |
| `scripts/funnel_daily_report.py` | 飞书推送脚本 |
| `agent_impl/frontend/dashboard.html` | 仪表盘页面 |
| `agent_impl/api/analytics.py` | 仪表盘后端 API |
| `agent_impl/api/track.py` | 埋点写入接口 |
| `agent_impl/frontend/scripts/tracker.js` | 前端埋点 SDK |
| `agent_impl/.env` | 所有秘钥（Supabase / 飞书 / LangSmith）|
| `supabase/migrations/create_events_table.sql` | events 表结构 |

---

## 未来要做的事（目前未做）

### 每天 10 点自动推飞书

目前要手动跑 `bash scripts/daily_dashboard.sh feishu`。

**自动化选项**：
1. **macOS 定时任务**（plist 文件已备好在 `scripts/com.crushe.dailyfunnel.plist`）——适合"我每天都开机"的场景
2. **GitHub Actions**（workflow 已写在 `.github/workflows/funnel-report.yml`）——跑在 GitHub 云上，不依赖本地电脑，但需要：
   - 确认 Zeabur 部署连的是哪个 GitHub 仓库和分支
   - 给对应仓库加 5 个 secret（SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / FEISHU_WEBHOOK_URL / FEISHU_SIGN_SECRET / DASHBOARD_URL）

**临时方案**：每天上班先跑一下手动命令就能看飞书。

### 部署到公网（让仪表盘 24/7 可用）

目前仪表盘只在 `localhost:8000` 本机可见。部署到 Zeabur 后任何人/任何设备都能访问 `https://<域名>/dashboard.html`。

**前提**：确认 Zeabur 连的是哪个仓库后，把本地改动推上去，Zeabur 会自动重新部署。

---

## 数据源

所有数据来自 **Supabase 的 `events` 表**，由以下两条路径写入：

- **前端埋点**：浏览器访问页面 → `tracker.js` → `POST /api/track` → 写 Supabase
- **后端埋点**：API 路由（analyze/report/chat）直接调用 `log_event_server` → 写 Supabase

如果 Supabase 暂时不可用，后端会自动 fallback 写入 `agent_impl/local_data/events.jsonl`（不会丢数据，但需要手动合并回 Supabase）。
