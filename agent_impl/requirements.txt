# 本文件仅是薄 shim：所有依赖和版本锁都在 pyproject.toml。
# 请不要在这里加包——改 pyproject.toml 的 [project] dependencies 或
# [project.optional-dependencies].test。
#
# 这份等价于"完整安装（含测试依赖）"，给 CI / 本地开发 / Zeabur 用。
# 运行时（不含测试）用 requirements.runtime.txt。
-e .[test]
