# LangGraph and LangChain
langgraph>=1.1.6,<2
langchain>=1.0.5,<2
langchain-openai>=0.2.0
langchain-deepseek>=0.1.0  # DeepSeek 官方集成，支持更稳定的 function calling
langgraph-checkpoint-postgres>=0.2.0
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0

# LangGraph CLI and SDK for deployment
langgraph-cli>=0.4.0
langgraph-sdk>=0.2.0
langgraph-prebuilt==1.0.8

# LangSmith for debugging and observability
langsmith>=0.1.0

# Environment and configuration
python-dotenv>=1.0.0
pydantic>=2.0.0

# Utilities
rich>=13.0.0  # 美化终端输出
typing-extensions>=4.0.0  # Python 3.11 compatibility for TypedDict
json-repair>=0.30.0  # 修复 LLM 生成的非法 JSON tool call 参数

# Web Server
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6  # 文件上传支持

# HTTP Client (for multimodal API)
httpx>=0.25.0

# Image processing (OCR resize)
Pillow>=10.0.0

# Authentication and Database
supabase>=2.0.0
PyJWT>=2.8.0
bcrypt>=4.0.0
