让我先仔细查看一些关键文件，确保表格内容完整准确：

[5 tools called]

现在我已经掌握了足够的细节，让我为你创建一份完整详细的上下文输入信息表格：

---

# 上下文工程输入信息全量梳理表

## 一、Layer 1: 静态情报区

| 层级 | 输入信息分类 | 输入字段 | 输入格式示例 | 输入 Prompt 逻辑 |
|:-----|:-------------|:---------|:-------------|:-----------------|
| Layer 1 | **用户信息 - 用户提供** | `user_info.user_provide` | `"姓名：小明\n年龄：26\n职业：程序员\n性格：内向"` | <ul><li>**Prompt 位置**：`{user_context}` 区块</li><li>**格式**：Markdown，标题 `#### 用户信息` → `##### 由用户提供的信息`</li><li>**逻辑**：全量输出，不压缩</li></ul> |
| Layer 1 | **用户信息 - 客观事实** | `user_info.fact` | `"用户朋友圈发了健身照（2026-01-10）"` | <ul><li>**Prompt 位置**：`{user_context}` 区块</li><li>**格式**：`##### 客观事实`</li><li>**逻辑**：全量输出，信任优先级最高</li></ul> |
| Layer 1 | **用户信息 - AI分析** | `user_info.ai_provide` | `"依恋类型：焦虑型\n核心优势：稳定工作、幽默感"` | <ul><li>**Prompt 位置**：`{user_context}` 区块</li><li>**格式**：`##### 军师分析得出的信息`</li><li>**逻辑**：由整理 Agent 从对话中提取写入</li></ul> |
| Layer 1 | **Crush 名称** | `crush_info.crush_name` | `"小美"` | <ul><li>**Prompt 位置**：`{user_context}` → `{crush_info}`</li><li>**格式**：`##### Crush 名称或昵称`</li><li>**逻辑**：如存在则单独展示</li></ul> |
| Layer 1 | **Crush信息 - 用户提供** | `crush_info.user_provide` | `"同事，比我大2岁，性格外向，爱运动"` | <ul><li>**Prompt 位置**：`{crush_info}`</li><li>**格式**：`##### 由用户提供的信息`</li><li>**逻辑**：全量输出，信任优先级最低</li></ul> |
| Layer 1 | **Crush信息 - 客观事实** | `crush_info.fact` | `"【聊天记录分析】\n- 平均回复时间：2小时\n- 主动率：30%"` | <ul><li>**Prompt 位置**：`{crush_info}`</li><li>**格式**：`##### 客观事实`</li><li>**逻辑**：从截图/聊天记录提取，最高可信</li></ul> |
| Layer 1 | **Crush信息 - AI分析** | `crush_info.ai_provide` | `"性格推断：回避型依恋\n当前态度：友好但保持距离"` | <ul><li>**Prompt 位置**：`{crush_info}`</li><li>**格式**：`##### 军师分析得出的信息`</li><li>**逻辑**：整理 Agent 推断写入</li></ul> |
| Layer 1 | **双方相处 - 用户提供** | `both_info.user_provide` | `"认识3个月，同一个项目组，每天都能见面"` | <ul><li>**Prompt 位置**：`{both_info}`</li><li>**格式**：`##### 由用户提供的信息`</li><li>**逻辑**：全量输出</li></ul> |
| Layer 1 | **双方相处 - 客观事实** | `both_info.fact` | `"最后一次单独约会：2026-01-05\n聊天频率：每天5-10条"` | <ul><li>**Prompt 位置**：`{both_info}`</li><li>**格式**：`##### 客观事实`</li><li>**逻辑**：从证据中提取</li></ul> |
| Layer 1 | **双方相处 - AI分析** | `both_info.ai_provide` | `"关系阶段：L2 吸引期\n核心阻力：用户过度主动，C溢出风险"` | <ul><li>**Prompt 位置**：`{both_info}`</li><li>**格式**：`##### 军师分析得出的信息`</li><li>**逻辑**：Status Agent 诊断后写入</li></ul> |

---

## 二、Layer 2: 工作上下文

| 层级 | 输入信息分类 | 输入字段 | 输入格式示例 | 输入 Prompt 逻辑 |
|:-----|:-------------|:---------|:-------------|:-----------------|
| Layer 2 | **现状报告 - 当前版本** | `current_status_report` | ```markdown\n## 🧭 情感罗盘\n### 1. 局势总览\n目前处于 L2 吸引期...\n### 2. 战场模型\n- 当前阶段：吸引期\n- 核心阻力：用户过度主动\n``` | <ul><li>**Prompt 位置**：`{status_report}`</li><li>**格式**：完整 Markdown（`report_content` 字段）</li><li>**逻辑**：输出 `current_status_report`，全量展开</li></ul> |
| Layer 2 | **现状报告 - 历史版本** | `status_report_history[]` | `"[最近] 上次判断为备胎边缘，C溢出严重\n[更早] L1初识期"` | <ul><li>**Prompt 位置**：`{history_summaries}`</li><li>**格式**：`#### 历史现状分析` + 列表</li><li>**逻辑**：最近2份输出 `summary`，更早的输出 `one_liner`</li></ul> |
| Layer 2 | **行动规划 - 当前版本** | `current_action_plan` | ```markdown\n## 推进蓝图\n**阶段性目标**：建立吸引力\n**核心策略**：冷冻降压...\n``` | <ul><li>**Prompt 位置**：`{action_plan}`</li><li>**格式**：完整 Markdown（`plan_content` 字段）</li><li>**逻辑**：输出 `current_action_plan`，全量展开</li></ul> |
| Layer 2 | **行动指南 - 进行中** | `action_guides[status="in_progress"]` | ```markdown\n## 📋 任务卡片：朋友圈人设建设\n### 🏷️ 军师简报\n- 任务目标：...\n### 👣 执行步骤\n1. ...\n``` | <ul><li>**Prompt 位置**：`{action_guides}` → `#### 当前进行中`</li><li>**格式**：完整 Markdown（`guide.guide_content`）</li><li>**逻辑**：**完整展开**，这是当前最重要的执行任务</li></ul> |
| Layer 2 | **行动指南 - 待执行/暂停** | `action_guides[status in ("pending", "paused")]` | `| abc123 | 冷冻降压 | pending | 减少主动联系频率 |` | <ul><li>**Prompt 位置**：`{action_guides}` → `#### 其他指南`</li><li>**格式**：Markdown 表格（ID/标题/状态/摘要）</li><li>**逻辑**：**仅元数据**，可通过工具按需加载详情</li></ul> |
| Layer 2 | **行动指南 - 已完成/取消** | `action_guides[status in ("completed", "cancelled", "expired")]` | `"[最近] 完成了朋友圈展示任务，crush点赞了\n[更早] 约咖啡成功"` | <ul><li>**Prompt 位置**：`{history_summaries}` → `#### 历史行动指南`</li><li>**格式**：最近2份中等摘要，更早的一句话</li><li>**逻辑**：归档后压缩，全量数据可抽屉查</li></ul> |
| Layer 2 | **任务绑定的行动指南详情** | `active_task.bound_action_guides[]` | ```markdown\n### 任务绑定的行动指南详情\n#### 指南 abc123: 朋友圈人设建设 (in_progress)\n完整内容...\n``` | <ul><li>**Prompt 位置**：`{bound_action_guides}`</li><li>**格式**：完整 Markdown</li><li>**逻辑**：当任务 `bind_action_guide_detail()` 后，后续轮次自动注入，最多3个</li></ul> |
| Layer 2 | **动态情报板** | `dynamic_intels[]` | ```markdown\n### 动态情报板 (请务必参考)\n#### 日程\n- 用户: 今晚8点要加班（有效期至 2026-01-14）\n#### 心情\n- Crush: 最近工作压力很大\n``` | <ul><li>**Prompt 位置**：`{action_guides}` 区块之后（Layer 2 部分）</li><li>**格式**：Markdown 按 category 分组</li><li>**逻辑**：只输出未过期的，无数量上限（⚠️ 潜在问题）</li></ul> |

---

## 三、Layer 3: 对话历史区（核心细节）

### 3.1 对话摘要

| 层级 | 输入信息分类 | 输入字段 | 输入格式示例 | 输入 Prompt 逻辑 |
|:-----|:-------------|:---------|:-------------|:-----------------|
| Layer 3 | **历史对话摘要** | `conversation_summaries[]` | ```xml\n<conversation_summaries>\n  <summary index="1" topics="约会,冷淡">昨天讨论了约会计划，用户担心crush态度冷淡...</summary>\n  <summary index="2" topics="聊天分析">分析了crush的回复特征，判断为保持距离...</summary>\n</conversation_summaries>\n``` | <ul><li>**Prompt 位置**：`{conversation_history}` 开头</li><li>**格式**：XML `<summary>` 标签，带 `index` 和 `topics` 属性</li><li>**逻辑**：最多输出最近5条，由整理 Agent 压缩生成</li><li>**触发**：对话轮次超过阈值时触发压缩</li></ul> |

### 3.2 任务思考过程（Rolling Scratchpad）

| 层级 | 输入信息分类 | 输入字段 | 输入格式示例 | 输入 Prompt 逻辑 |
|:-----|:-------------|:---------|:-------------|:-----------------|
| Layer 3 | **任务思考摘要** | `active_task.summary` | `"之前的推理：用户提供的新证据表明crush态度有所回暖..."` | <ul><li>**Prompt 位置**：`{conversation_history}` → `<task_scratchpad>`</li><li>**格式**：XML `<summary>` 标签</li><li>**逻辑**：当 reasoning 条目 >10 条时，旧的被压缩为 summary</li></ul> |
| Layer 3 | **任务思考笔记** | `active_task.reasoning[]` | ```xml\n<task_scratchpad task="判断crush是否喜欢用户">\n  <summary>之前的推理总结...</summary>\n  <note index="1">用户提供了新的聊天截图，crush主动开启话题</note>\n  <note index="2">结合之前的分析，态度确实有回暖迹象</note>\n</task_scratchpad>\n``` | <ul><li>**Prompt 位置**：`{conversation_history}` → `<task_scratchpad>`</li><li>**格式**：XML `<note>` 标签，带 `index`</li><li>**逻辑**：只输出**当前活跃任务**的笔记，最多10条</li><li>**来源**：Agent 输出 JSON 中的 `task_update.reasoning_note`</li></ul> |

### 3.3 对话消息（最近 25 条）

| 层级 | 输入信息分类 | 输入字段 | 输入格式示例 | 输入 Prompt 逻辑 |
|:-----|:-------------|:---------|:-------------|:-----------------|
| Layer 3 | **用户消息 - 纯文本** | `messages[role="user"].content` | ```xml\n<turn index="1" task="判断crush是否喜欢用户">\n  <user>他今天主动找我聊天了！发了张自拍给我</user>\n  ...\n</turn>\n``` | <ul><li>**Prompt 位置**：`{conversation_history}` → `<recent_turns>` → `<turn>` → `<user>`</li><li>**格式**：XML `<user>` 标签，内容做 XML 转义</li><li>**逻辑**：按时间顺序输出，每条用户消息开启新 turn</li></ul> |
| Layer 3 | **用户消息 - 带图片** | `messages[role="user"].content` (含图片描述) | `<user>[图片] 这是他发给我的聊天记录截图</user>` | <ul><li>**Prompt 位置**：同上</li><li>**格式**：图片在 content 中以 `[图片]` 或实际图片 URL 形式存在</li><li>**逻辑**：图片处理由 `image_processor.py` 负责，转为描述或保留 URL</li></ul> |
| Layer 3 | **AI 消息 - 对用户回复** | `messages[role="assistant"].content` → 提取 `response` 字段 | ```xml\n<turn index="1" task="...">\n  <user>...</user>\n  <assistant>这是个好迹象！他主动发自拍说明对你有好感，我来帮你分析一下...</assistant>\n</turn>\n``` | <ul><li>**Prompt 位置**：`<recent_turns>` → `<turn>` → `<assistant>`</li><li>**格式**：XML `<assistant>` 标签</li><li>**逻辑**：从 LLM 输出 JSON 中提取 `response` 字段，不含 `thought`、`inquiry_card` 等内部字段</li></ul> |
| Layer 3 | **AI 消息 - 内部思考** | `messages[role="assistant"]` 中的 `thought` 字段或 `task_update.reasoning_note` | ```xml\n<turn index="1" task="判断crush是否喜欢用户">\n  <user>...</user>\n  <thought>这是一个积极信号，需要更新判断。用户情绪高涨，应先给予肯定再分析。</thought>\n  <assistant>...</assistant>\n</turn>\n``` | <ul><li>**Prompt 位置**：`<turn>` → `<thought>`</li><li>**格式**：XML `<thought>` 标签</li><li>**逻辑**：⚠️ **只有同任务的 thought 才会显示**<br>即 `msg.task_id == current_task_id` 时才输出</li><li>**来源**：LLM 输出 JSON 的 `thought` 或 `task_update.reasoning_note`</li></ul> |
| Layer 3 | **AI 消息 - 提问卡片** | `messages[role="assistant"]` 中的 `inquiry_card` 字段 | ```xml\n<turn index="2" task="...">\n  <user>...</user>\n  <assistant>在给你建议之前，我想再了解一下：\n【提问】\n- 他回消息慢的时候，会解释自己在干嘛吗？</assistant>\n</turn>\n``` | <ul><li>**Prompt 位置**：同 `<assistant>`</li><li>**格式**：`inquiry_card` 被格式化为文本，如 `【提问】\n- 问题1\n- 问题2`</li><li>**逻辑**：`_format_assistant_message_for_history()` 函数处理，提取 `inquiry_card.questions[].question`</li></ul> |
| Layer 3 | **AI 消息 - 报告产出通知** | `messages[role="assistant"]` 中的系统通知 | `<assistant>（报告1已生成）</assistant>` | <ul><li>**Prompt 位置**：同 `<assistant>`</li><li>**格式**：括号包裹的简短通知</li><li>**逻辑**：当 Status/Plan/Guide Agent 产出时，系统自动插入通知消息</li></ul> |
| Layer 3 | **AI 消息 - 工具调用（发起）** | `messages[role="assistant"]` 中带 `tool_calls` 字段 | `<assistant>[已发起工具调用: load_inquiry_skill_instructions]</assistant>` | <ul><li>**Prompt 位置**：同 `<assistant>`</li><li>**格式**：`[已发起工具调用: 工具名]`</li><li>**逻辑**：`get_msg_role_and_content()` 处理，提取 `tool_calls[].name` 格式化</li></ul> |
| Layer 3 | **工具返回消息** | `messages[role="tool"]` | ⚠️ **不输出到对话历史** | <ul><li>**Prompt 位置**：**不在 `<recent_turns>` 中渲染**</li><li>**逻辑**：工具返回内容仍保留在 `state.messages` 中，但 `_format_interleaved_history()` 跳过 `role="tool"` 的消息</li><li>**原因**：避免跨轮次累计导致上下文膨胀</li></ul> |
| Layer 3 | **Agent 间传递指令** | `state.instruction` | `"用户提供了新截图，侧重分析是否改变了备胎定性"` | <ul><li>**Prompt 位置**：`{instruction}` (仅子 Agent 可见)</li><li>**格式**：纯文本</li><li>**逻辑**：Main Agent 调用子 Agent 时设置，作为"CEO Brief"</li></ul> |

### 3.4 任务系统

| 层级 | 输入信息分类 | 输入字段 | 输入格式示例 | 输入 Prompt 逻辑 |
|:-----|:-------------|:---------|:-------------|:-----------------|
| Layer 3 | **任务列表** | `task_registry[agent_name][]` | ```markdown\n| task_id | summary | status | active |\n|---------|---------|--------|--------|\n| 判断crush是否喜欢用户 | 分析crush的态度和意图 | active | ✅ |\n| 朋友圈人设建设 | 第一阶段展示价值 | pending |  |\n``` | <ul><li>**Prompt 位置**：`{task_index}`</li><li>**格式**：Markdown 表格</li><li>**逻辑**：按时间倒序展示最近5个任务，当前活跃任务标 ✅</li></ul> |
| Layer 3 | **活跃任务详情** | `active_task` (from `task_registry`) | ```json\n{\n  "task_id": "判断crush是否喜欢用户",\n  "summary": "分析crush的态度和意图",\n  "reasoning_notes": ["用户提供了新的聊天截图...", "分析表明态度回暖..."]\n}\n``` | <ul><li>**Prompt 位置**：`{active_task_payload}`</li><li>**格式**：JSON 字符串</li><li>**逻辑**：包含 `task_id`、`summary`、`reasoning_notes`（最近10条）</li></ul> |
| Layer 3 | **当前任务 ID** | `current_task_id` | `"判断crush是否喜欢用户"` | <ul><li>**Prompt 位置**：`{current_task_id}`</li><li>**格式**：纯文本</li><li>**逻辑**：用于输出 JSON 时填充 `task_id` 字段</li></ul> |

---

## 四、Layer 0: 系统指令（Prompt 模板）

| 层级 | 输入信息分类 | 输入字段 | 输入格式示例 | 输入 Prompt 逻辑 |
|:-----|:-------------|:---------|:-------------|:-----------------|
| Layer 0 | **角色定义** | Prompt 模板固定内容 | `"你是**小话**，用户的**专属恋爱军师**..."` | <ul><li>**Prompt 位置**：Prompt 文件开头</li><li>**格式**：Markdown</li><li>**逻辑**：只读常驻，每次调用都完整输入</li></ul> |
| Layer 0 | **核心法则（ACR/L-T）** | Prompt 模板固定内容 | `"## 核心底层法则：P.M.P 局势模型\n### 核心度量衡：ACR\n..."` | <ul><li>**Prompt 位置**：Prompt 文件中间</li><li>**格式**：Markdown</li><li>**逻辑**：只读常驻</li></ul> |
| Layer 0 | **信任优先级** | Prompt 模板固定内容 | `"1. 最高可信 - 客观事实\n2. 中等可信 - AI分析\n3. 最低可信 - 用户提供"` | <ul><li>**Prompt 位置**：`{user_context}` 或独立区块</li><li>**格式**：Markdown 列表</li><li>**逻辑**：只读常驻</li></ul> |
| Layer 0 | **输出格式规范** | Prompt 模板固定内容 | `"## 输出格式 (JSON)\n```json\n{\n  \"task_id\": \"...\",\n  \"thought\": \"...\",\n  ...\n}\n```"` | <ul><li>**Prompt 位置**：Prompt 文件末尾</li><li>**格式**：Markdown + JSON Schema</li><li>**逻辑**：只读常驻，定义 Agent 输出结构</li></ul> |
| Layer 0 | **技能指令（动态加载）** | `{skills_prompt}` 或 `{inquiry_skill_metadata}` | `"# Inquiry Skill Protocol\n本模块定义了生成 inquiry_card 的标准协议..."` | <ul><li>**Prompt 位置**：Prompt 文件末尾或中间占位符</li><li>**格式**：Markdown</li><li>**逻辑**：二阶段协议：Agent 调用 `load_inquiry_skill_instructions()` 后，工具返回指令注入此处</li></ul> |

---

## 五、完整 XML 对话历史格式示例

```xml
<conversation_history>
  <!-- 历史摘要（最多5条） -->
  <conversation_summaries>
    <summary index="1" topics="约会,冷淡">昨天讨论了约会计划，用户担心crush态度冷淡，军师建议观察并给出了分析</summary>
    <summary index="2" topics="聊天分析">分析了crush的回复特征，判断为L2吸引期，核心阻力是用户过度主动</summary>
  </conversation_summaries>
  
  <!-- 当前任务的思考过程（Rolling Scratchpad） -->
  <task_scratchpad task="判断crush是否喜欢用户">
    <summary>之前的推理：crush回复频率不稳定，但内容质量尚可，初步判断为有兴趣但不确定</summary>
    <note index="1">用户提供了新的聊天截图，crush主动开启话题问周末计划</note>
    <note index="2">结合之前的分析，这是一个积极信号，态度确实有回暖迹象</note>
  </task_scratchpad>
  
  <!-- 最近对话（最多25条消息） -->
  <recent_turns>
    <turn index="1" task="判断crush是否喜欢用户">
      <user>他今天主动找我聊天了！问我周末有什么安排</user>
      <thought>这是一个积极信号。crush主动开启话题且关心用户时间安排，说明有进一步发展的意愿。需要引导用户正确回应。</thought>
      <assistant>哦？这是个好迹象！他主动问你周末安排，说明他在试探能不能约你。你先别急着回，我来帮你分析一下怎么回复效果最好。</assistant>
    </turn>
    <turn index="2" task="判断crush是否喜欢用户">
      <user>那我应该怎么回他？</user>
      <assistant>在给你建议之前，我想再了解一下：
【提问】
- 他问周末安排的时候，语气是怎样的？是随口一问还是很认真的样子？</assistant>
    </turn>
    <turn index="3" task="判断crush是否喜欢用户">
      <user>感觉挺认真的，还发了个表情</user>
      <thought>用户反馈crush态度认真，这进一步确认了积极信号。现在可以给出具体的回复建议。</thought>
      <assistant>那就更好了！发表情说明他希望营造轻松的氛围。建议你这样回...</assistant>
    </turn>
  </recent_turns>
</conversation_history>
```

---

## 六、各类消息的详细处理逻辑

| 消息类型 | 原始存储位置 | 提取/格式化函数 | 输出位置 | 输出格式 | 特殊处理 |
|:---------|:-------------|:----------------|:---------|:---------|:---------|
| **用户文本消息** | `messages[role="user"]` | `get_msg_role_and_content()` | `<user>` | 纯文本，XML 转义 | 无 |
| **用户图片消息** | `messages[role="user"]` | `image_processor.py` | `<user>` | `[图片] + 描述` 或 Base64 | 图片处理后替换 content |
| **AI 回复（response）** | `messages[role="assistant"]` → JSON → `response` | `_format_assistant_message_for_history()` | `<assistant>` | 纯文本 | 从 JSON 中提取 `response` 字段 |
| **AI 内部思考（thought）** | `messages[role="assistant"]` → JSON → `thought` | `_format_interleaved_history()` | `<thought>` | 纯文本 | ⚠️ 只有 `task_id` 匹配时才输出 |
| **AI 提问卡片** | `messages[role="assistant"]` → JSON → `inquiry_card` | `_format_assistant_message_for_history()` | `<assistant>` 内 | `【提问】\n- 问题1\n- 问题2` | 仅提取 `questions[].question` |
| **AI 报告通知** | 系统注入 `messages` | 直接使用 | `<assistant>` | `（报告N已生成）` | 简短通知格式 |
| **AI 工具调用（发起）** | `messages[role="assistant"].tool_calls` | `get_msg_role_and_content()` | `<assistant>` | `[已发起工具调用: xxx]` | 提取工具名格式化 |
| **工具返回** | `messages[role="tool"]` | ❌ 不处理 | ❌ 不输出 | - | 避免上下文膨胀 |
| **Agent 间指令** | `state.instruction` | 直接注入 | `{instruction}` | 纯文本 | 仅子 Agent Prompt 可见 |
| **任务思考笔记** | `task_registry[agent].reasoning[]` | `_format_interleaved_history()` | `<task_scratchpad>` → `<note>` | 纯文本 | 仅当前活跃任务输出 |
| **任务思考摘要** | `task_registry[agent].summary` | `_format_interleaved_history()` | `<task_scratchpad>` → `<summary>` | 纯文本 | 压缩后的历史思考 |
| **对话摘要** | `conversation_summaries[]` | `_format_interleaved_history()` | `<conversation_summaries>` → `<summary>` | 带 topics 属性 | 压缩后的历史对话 |

---

## 七、Token 预算分配

| 层级 | 预算（Token） | 主要内容 | 压缩策略 |
|:-----|:-------------|:---------|:---------|
| Layer 0 | 4,000 | 角色定义、法则、输出格式 | 不压缩 |
| Layer 1 | 15,000 | 3×3 情报矩阵 | 目前全量输出，TODO：语义召回 |
| Layer 2 | 20,000 | 报告/规划/指南/动态情报 | 渐进披露 + 历史摘要 |
| Layer 3 | 15,000 | 摘要 + 最近25条消息 + 思考 | 超阈值触发压缩 |
| 输出预留 | 8,000 | 模型输出 | - |
| **合计** | **70,000** | - | - |

---

这份表格完整梳理了你系统中所有的上下文输入信息。基于这个表格，你可以清晰地看到：
1. 哪些信息是**必须保留**的（如当前报告、进行中的指南）
2. 哪些信息**可以精简**（如 XML 标签、工具返回）
3. 哪些信息**缺乏控制**（如动态情报无数量限制）

你希望我针对哪些具体项目给出优化建议？